# Dedicated PostgreSQL connection used only by Lab 2 tests and E2E.
DATABASE_URL="postgresql://replace-user:replace-password@localhost:5434/toktickit_test?schema=public"

# Docker Compose test database settings. Copy this file to .env.test and replace values.
POSTGRES_USER="replace-user"
POSTGRES_PASSWORD="replace-password"
POSTGRES_DB="toktickit_test"
